package com.pentaho.ctools.utils;

public class PUCSettings {
  // When SHOWHIDDENFILES is true it means that hidden files are being shown in the repository
  // When SHOWHIDDENFILES is false it means that hidden files are NOT being shown in the repository
  public static boolean SHOWHIDDENFILES = false;

}
